# ===========================================
# PhotoStudio Backend Configuration (Example)
# ===========================================

# PostgreSQL (local docker-compose)
DATABASE_URL=postgres://photostudio:photostudio123@localhost:5432/photostudio?sslmode=disable

# JWT/Auth (CHANGE IN PROD)
JWT_SECRET=change-me-jwt-secret
JWT_ACCESS_TTL=15m
JWT_ALLOW_LEGACY_CLAIMS=true
REFRESH_TTL=168h
REFRESH_TOKEN_PEPPER=change-me-refresh-pepper
VERIFICATION_CODE_PEPPER=change-me-verification-pepper
VERIFY_CODE_TTL=5m
VERIFY_RESEND_COOLDOWN=60s

# Auth cookies
COOKIE_SECURE=false
COOKIE_SAMESITE=Lax
# For cross-site frontend<->api on different eTLD+1 use: COOKIE_SAMESITE=None and COOKIE_SECURE=true
# COOKIE_SAMESITE=None
COOKIE_PATH=/api/v1/auth

# Server
PORT=8090

# Environment
ENV=development
# APP_ENV takes precedence over ENV when both are set
APP_ENV=dev

# CORS (comma-separated exact origins; required for cross-domain cookies)
CORS_ALLOWED_ORIGINS=http://localhost:3000

# Run periodic cleanup:
# go run cmd/auth_cleanup/main.go
