# ===========================================
# PhotoStudio Backend Configuration (Example)
# ===========================================

# PostgreSQL (local docker-compose)
DATABASE_URL=postgres://photostudio:photostudio123@localhost:5432/photostudio?sslmode=disable

# JWT (CHANGE IN PROD)
JWT_SECRET=your_super_secret_jwt_key_here_min_32_characters

# Token TTLs (align with MWork requirements)
# Access token: 15 minutes, Refresh token: 7 days
ACCESS_TOKEN_TTL=15m
REFRESH_TOKEN_TTL=168h

# Refresh cookie options
# For local dev you can keep defaults.
# COOKIE_SECURE=true is required when COOKIE_SAMESITE=none
COOKIE_DOMAIN=
COOKIE_SECURE=false
COOKIE_SAMESITE=lax

# Server
PORT=3001

# Environment
ENV=development
