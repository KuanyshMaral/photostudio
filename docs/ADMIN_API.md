# 🛡 Admin API Documentation

Документация для административных API эндпоинтов платформы **StudioBooking**.

Все эндпоинты в этом разделе:
- 🔐 требуют **роль `admin`**
- 🔑 требуют **валидный JWT токен**
- 📍 доступны по префиксу: `/api/v1/admin`

---

## 🔐 Аутентификация

Все запросы должны содержать заголовок:

```http
Authorization: Bearer <JWT_TOKEN>
````

Если пользователь не имеет роли `admin`, сервер вернёт:

```json
{
  "success": false,
  "error": "forbidden"
}
```

---

## 🏢 Студии на модерации

### 📌 Получить список студий со статусом `pending`

**GET** `/api/v1/admin/studios/pending`

#### Query параметры

| Параметр | Тип    | Обязательный | Описание                                            |
| -------- | ------ | ------------ | --------------------------------------------------- |
| page     | number | ❌            | Номер страницы (по умолчанию: 1)                    |
| limit    | number | ❌            | Количество элементов на странице (по умолчанию: 20) |

#### Пример запроса

```http
GET /api/v1/admin/studios/pending?page=1&limit=20
```

#### Пример ответа

```json
{
  "success": true,
  "data": {
    "studios": [
      {
        "id": 12,
        "name": "Loft Studio",
        "owner_id": 7,
        "status": "pending",
        "created_at": "2026-01-07T12:30:00Z"
      }
    ],
    "total": 5,
    "page": 1,
    "limit": 20
  }
}
```

---

## ✅ Верификация студии

### 📌 Подтвердить студию

**POST** `/api/v1/admin/studios/:id/verify`

#### Path параметры

| Параметр | Тип    | Описание  |
| -------- | ------ | --------- |
| id       | number | ID студии |

#### Тело запроса

```json
{
  "admin_notes": "Документы в порядке"
}
```

#### Пример ответа

```json
{
  "success": true,
  "message": "Studio verified successfully"
}
```

---

## ❌ Отклонение студии

### 📌 Отклонить студию

**POST** `/api/v1/admin/studios/:id/reject`

#### Path параметры

| Параметр | Тип    | Описание  |
| -------- | ------ | --------- |
| id       | number | ID студии |

#### Тело запроса

```json
{
  "reason": "Неполный пакет документов"
}
```

#### Пример ответа

```json
{
  "success": true,
  "message": "Studio rejected"
}
```

---

## 📊 Статистика платформы

### 📌 Получить общую статистику

**GET** `/api/v1/admin/statistics`

#### Пример запроса

```http
GET /api/v1/admin/statistics
```

#### Пример ответа

```json
{
  "success": true,
  "data": {
    "total_users": 150,
    "total_studios": 45,
    "total_bookings": 320,
    "pending_studios": 8,
    "today_bookings": 12
  }
}
```

---

## ⚠️ Возможные ошибки

### 401 Unauthorized

```json
{
  "success": false,
  "error": "unauthorized"
}
```

### 403 Forbidden

```json
{
  "success": false,
  "error": "forbidden"
}
```

### 404 Not Found

```json
{
  "success": false,
  "error": "studio not found"
}
```

### 400 Bad Request

```json
{
  "success": false,
  "error": "invalid request payload"
}
```

---

## 🧪 Примечания

* Все даты возвращаются в формате **ISO 8601 (UTC)**
* Все ответы имеют единый формат `{ success, data | error }`
* Endpoints доступны только администраторам

```
